# 版本发布说明 (Release Notes)

---

## [v1.1.0] - 2026-09-24

### 🚀 新增功能与重大升级 (New Features & Major Upgrades)
- **一周 21 轮完全不重样的酷炫飞书日报系统**：
  - **动态 Persona 算法**：根据运行时的星期与早/午/晚时段（每周 21 个独立轮次），自动匹配 21 种截然不同、风格酷炫且带点小调皮的特工播报角色。
  - **丰富表情包与幽默梗**：熟练运用打工人、特工、吃瓜、疯狂星期四、赛博朋克等趣味 Emoji 与网络梗，彻底告别单调枯燥的通知模版。
  - **全场景独特性保证**：确保每周 21 轮定时运行中，每一轮推送标题、开场白、状态感言与落款均 100% 独一无二。
- **超详细 P0 紧急报警与问题排查推送**：
  - **更丰富的报警信息**：遇到 Cookie 失效、验证码风控、登录拦截或首次运行为空等异常时，推送包含故障时间、错误归类（如 `COOKIE_EXPIRED` / `CAPTCHA_RISK_CONTROL`）、根因诊断与逐步修复指导的详细卡片。
  - **失败明细排查**：当个别作品下载或网盘上传失败时，在日报中附带失败作品的具体 ID、错误类型与排查提示，方便用户快速定位问题。

---

### 🛠️ 文档重构与体验优化 (Improvements & Refactoring)
- **README 全面重构与内容扩充**：
  - **充满人情味与热情的自述风格**：采用酷酷的、带着小调皮的大白话文风重新编译 `README.md`，以作者真心推荐自己爱用项目的角度重新书写。
  - **新增项目背景与架构图**：补充为什么写这个项目的故事背景、大白话数字搬运特工工作原理架构图、以及更加清晰全面的 4 步小白零基础部署教程与 FAQ 指南。
- **版本号迭代**：
  - 正式将项目版本号升级迭代至 `v1.1.0`。

---

## [v1.0.2] - 2026-09-24

### 🛠️ 优化与修复 (Improvements & Fixes)
- **GitHub Actions 定时任务（Cron）延时问题优化与说明**：
  - **延时原因明确**：明确告知用户 GitHub Actions 官方定时任务延时是由 GitHub 平台服务器整点排队拥堵引起的已知现象，非程序代码故障。
  - **Cron 时间点错峰优化**：修改 `.github/workflows/douyin-daily.yml` 中的 Cron 定时配置，将原整点触发时间（08:00、12:00、20:00）调整为错峰非整点时间（08:17、12:23、20:37），大幅避开全球整点排队高峰，有效缓解定时任务延时问题。
  - **FAQ 常见问题解答更新**：在 `README.md` 中新增关于 GitHub 定时任务延时原因及手动触发补救方案的大白话说明。

---

## [v1.0.1] - 2026-09-24

### 🚀 新增功能 (New Features)
- **支持 Repository Variables 配置博主链接**：
  - 工作流 `.github/workflows/douyin-daily.yml` 支持使用 `vars.DOUYIN_URL` 读取博主链接，并自动降级回退至 `secrets.DOUYIN_URL`，方便用户在 GitHub 后台非敏感变量区配置和管理博主主页地址。
- **智能链接提取与杂质过滤 (口令与混合文本兼容)**：
  - 新增 `parse_douyin_urls` 函数，支持直接粘贴抖音 App 复制的分享口令文字（例如包含“复制打开抖音…”的文字）。
  - 自动从复杂文本中通过正则表达式精准提取出合法的 `http://` 或 `https://` 网址，并去除参数与尾部中英文标点符号。
  - 兼容空格、换行、中文逗号（`，`）、英文逗号（`,`）、分号（`；`/`;`）等多种分隔格式，自动去重。

---

### 🐛 修复的 Bug (Bug Fixes)
- **链接解析报错与非法字符分割问题**：
  - 修复了此前在 `DOUYIN_URL` 中粘贴抖音 App 包含中文描述的分享链接时，导致 URL 解析错误或无法正常访问博主主页的问题。
- **爬虫页面打开与滚动健壮性提升**：
  - 修复了在多博主循环抓取过程中，若某个博主主页加载超时或 DOM 滚动定位失败时导致整个程序崩溃退出的问题，增加了 `try-except` 异常捕获与容错提示。

---

### 🛠️ 优化与重构 (Improvements)
- **环境变量安全获取机制**：
  - 将 `main.py` 中的环境变量读取改为 `os.environ.get()` 安全获取方式，避免因个别非必填环境变量未配置而直接触发 `KeyError` 字典报错。
- **文档说明更新**：
  - 同步更新 `README.md` 中关于 `DOUYIN_URL` 获取、支持粘贴 App 分享口令以及 GitHub Repository Variables 的配置指导。

---

## [v1.0.0] - 2026-09-24

### 🚀 新增功能 (New Features)
- **多类型作品全自动备份**：
  - **视频作品备份**：自动抓取抖音博主最新发布的短视频，支持结合 `knock.py` 探测并自动下载 1080P 超高清画质视频。
  - **图集作品备份**：自动提取高清无水印图集图片，支持导出最高画质原图。
  - **实况动图（Live Photo）备份**：自动识别并单独保存图集中包含的 Live Photo 动态视频文件。
  - **作品封面与博主头像备份**：自动保存单个作品的高清封面，并在博主更换头像时自动同步最新的高清头像（支持 1080x1080 尺寸）。
- **WebDAV 网盘自动归档与智能去重**：
  - 支持将备份文件上传至坚果云、Alist、NAS 等 WebDAV 网盘。
  - 按博主昵称自动建立专属目录（`douyin/博主昵称/`），文件名包含发布时间与作品标题。
  - 具备智能增量去重机制，利用 `history.json` 自动跳过已备份作品，节省网盘空间与网络带宽。
- **飞书机器人消息推送与异常预警**：
  - 运行完成后自动向飞书群推送中文日报通知，清晰明了展示新增、跳过和失败明细。
  - 遇到 Cookie 过期或触发抖音验证码/风控拦截时，自动发送预警通知并安全暂停，保障账号安全。
- **GitHub Actions 云端定时自动化工作流**：
  - 提供 `.github/workflows/douyin-daily.yml`，支持每天北京时间 08:00、12:00、20:00 无需开启电脑或服务器全自动云端运行。

---

### 🐛 修复的 Bug (Bug Fixes)
- **风控与 Cookie 失效拦截处理**：
  - 修复了遇到强制登录弹窗或 API 状态码异常时脚本盲目重试的问题，新增状态码与页面元素检测，避免触发更严厉的风控。
- **长标题与特殊字符非法路径报错**：
  - 修复了博主昵称或作品描述中包含特殊符号/非法字符时导致 WebDAV 路径创建失败或本地文件报错的问题，加入了正则表达式安全清洗机制。
- **重名作品覆盖问题**：
  - 修复了同一博主同一天发布多个同名或无标题作品时文件相互覆盖的问题，增加了重名自动递增后缀 `(1)`, `(2)` 机制。

---

### 🛠️ 优化与重构 (Improvements)
- **新手友情文档与大白话小白教程**：
  - 重新整理并极大丰富了 `README.md`，添加了 4 步零基础图文/表格部署教程、参数获取说明与 FAQ 常见问题解答，即使没有编程经验的用户也能轻松上手。
- **代码可读性与中文注释重构**：
  - 对 `main.py` 与 `knock.py` 进行全面结构清理与优化，添加了通俗易懂的中文大白话注释与规范的文档字符串（Docstring），降低二次开发与维护门槛。
- **画质探测与重试机制**：
  - 优化了 Playwright 隐形浏览器的模拟滚动与抓取逻辑，增加随机等待延时，降低被封禁风险；重构了 `knock.py` 1080P 超高清画质探测与提取逻辑。

---

### ⚠️ 破坏性更新或注意事项 (Breaking Changes / Notes)
- **环境变量配置要求**：
  - 必须正确配置 `DOUYIN_COOKIE` 与 `DOUYIN_URL`，若 Cookie 过期需及时在 GitHub Secrets 中更新，否则任务将终止并报警。
- **环境依赖要求**：
  - 本地运行需安装 Python 3.8+ 及依赖库（`requests`, `playwright`），并完成 Playwright Chromium 浏览器驱动的安装 (`playwright install chromium`)。
- **WebDAV 目录格式改变**：
  - 作品保存路径统一规整为 `douyin/博主昵称/`。若以往自定义过其他目录格式，请注意搬迁或配置兼容。
