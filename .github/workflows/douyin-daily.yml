name: douyin-daily

on:
  schedule:
    - cron: '17 22 * * *'  # 北京时间 06:17 (每天 6 点档，提前 2 小时，错峰执行)
    - cron: '23 2 * * *'   # 北京时间 10:23 (每天 10 点档，提前 2 小时，错峰执行)
    - cron: '37 10 * * *'  # 北京时间 18:37 (每天 18 点档，提前 2 小时，错峰执行)
  workflow_dispatch:
    inputs:
      max:
        description: '本轮最多处理作品数'
        required: false
        default: '30'

permissions:
  contents: write

concurrency:
  group: douyin-daily
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: |
          pip install -r requirements.txt
          playwright install chromium --with-deps
      - name: Run
        env:
          DOUYIN_COOKIE: ${{ secrets.DOUYIN_COOKIE }}
          DOUYIN_URL: ${{ vars.DOUYIN_URL || secrets.DOUYIN_URL }}
          WEBDAV_URL: ${{ secrets.WEBDAV_URL }}
          WEBDAV_USER: ${{ secrets.WEBDAV_USER }}
          WEBDAV_PASS: ${{ secrets.WEBDAV_PASS }}
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          MAX_PER_RUN: ${{ github.event.inputs.max || '30' }}
        run: python main.py
      - name: Commit history
        if: always()
        run: |
          git config user.name "douyin-bot"
          git config user.email "bot@noreply.github.com"
          git add history.json
          git diff --cached --quiet || git commit -m "chore: update history"
          git push || true
